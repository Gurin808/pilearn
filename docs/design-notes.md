# PILearn design notes

This file is a working record. It started on 2026-09-21, after reading Deslauriers et al. (2019) and Kestin et al. (2025), and gets updated whenever a session changes a design decision. The name is PILearn in prose and `pilearn` for the binary and paths. The author and Feynman sessions both edit it.

Every design choice traces to evidence, a paper from the reading list or another checked source, or is marked `OPEN`. Decisions go into the log at the bottom, which is append-only. Nothing there is rewritten without a new entry saying so.

## Goal

A learning tool on Pi: an AI tutor that works from a book, paper, or course, and installs the way Feynman does, so anyone can set it up. It should be good enough to recommend to students. It is built from the learning-science literature and judged by what the learner can recall, not by how sessions feel.

## Principles

| # | Principle | Evidence |
|---|---|---|
| P1 | The learner tries before being told. | Deslauriers 2019, where the manipulation was trying first versus being shown the solution. The pretesting effect, Kornell, Hays & Bjork 2009. |
| P2 | Free recall before multiple choice. Recognizing is too easy, and recalling is where learning happens. | Testing-effect literature, Roediger & Karpicke 2006. Desirable difficulties, Bjork & Bjork 2011. |
| P3 | Pre-questions before each chunk, written from that chunk. | Pan & Carpenter 2023, *Educ. Psychol. Rev.* 35:97. |
| P4 | Tools and structure enforce the sequence, not the prompt alone, because models drift on problems with many parts. | Kestin 2025: a system prompt alone wasn't enough, so the platform guided students step by step. |
| P5 | The tutor never produces the ground truth. The book text and expert solutions are given to it. | Kestin 2025: 83% rated explanations at least as good as a human's because the prompts carried expert answers. |
| P6 | Quick feedback and self-pacing are what classrooms can't scale, and what the tool does well. | Kestin 2025, best practices (vi) and (vii). |
| P7 | Judge the tool by retention over time, never by how a session felt. | Deslauriers 2019: for novices, the feeling of learning runs against actual learning. |
| P8 | Unguided chat is the failure mode. Structure is what makes it work. | Kestin 2025, citing Krupp 2023 and Forero 2023. |
| P9 | Assess early and make the result visible. | Deslauriers 2019, Discussion. |
| P10 | Subagents run async, so the learner sees their progress while working. pi-subagents' progress widget does this. | The author's earlier Pi setup, observed 2026-09-21. |
| P11 | Anki handles recall of single items, like definitions, steps, and equations. Synthesis stays in tutor sessions. Measure retention, never cards reviewed. Cap new cards per session. | Roediger & Karpicke 2006; Roediger & Butler 2011; Bjork & Bjork 2011. |
| P12 | Sessions are interactive and constructive in ICAP's sense, delivered in a guided sequence with worked examples. Never unguided discovery. | Chi & Wylie 2014; Kirschner, Sweller & Clark 2006; Kestin 2025. |
| P13 | Test, don't re-present. Once an item is recalled, more study adds little and more testing drives retention, so the tool asks instead of rereading. | Karpicke & Roediger 2008; Roediger & Karpicke 2006; Roediger & Butler 2011. |
| P14 | Spacing, interleaving, and varied practice go into the review schedule. They feel harder now and last longer. Evaluate long-term retention, never immediate performance. | Bjork & Bjork 2011. Extends P7. |
| P15 | Understanding is the goal and hard problems are the means. The aim is the bigger picture: connections, several views of one problem, and being able to explain it. Speed at calculation isn't the aim. Hard problems are chosen for the idea they train. The exception is a field's basic moves, which need fluency because fluency frees attention. Exercises are done by hand, chosen on purpose, weighted toward what the learner struggled with, and sized by a per-course `practice` setting, `foundational` or `conceptual`. | The author's view, consistent with Bjork & Bjork 2011; Kapur 2008, 2016; Sinha & Kapur 2021; Chi et al. 1989; Rittle-Johnson, Schneider & Star 2015; Bastani et al. 2025. |

## How it's built now

```
~/study/                       level 1: adds books, plans across courses
└── courses/<course>/          level 2: the tutor for one book
      ├── course.json          id, title, source, pageOffset, practice, solutions
      ├── source.pdf           link to the book
      ├── progress.md          sessions, exercises, recall log, next block
      ├── sessions/            one log per session
      └── chapters/<chNN>/     level 3: a chapter-reader writes digest.md here
```

- Each level has its own `AGENTS.md`. Pi loads all of them from the workspace root down, and the deepest one sets the role. A shared `AGENTS.md` in the agent dir holds the five teaching rules that apply in every answer.
- The `chapter-reader` subagent reads one chapter's pages and writes a digest organized by section, with math taken from page images as LaTeX. No single context ever holds the whole book.
- The tutor reads the whole chapter digest every session but teaches a block of about 1 to 4 sections. The session has eight steps: orient, warm-up, pre-test, read, recall and post-test, practice, cards, log.
- Tools do what the prompt can't guarantee. `pilearn_scaffold` creates folders, `pilearn_anki` caps cards at 8 per session, `pilearn-guard` keeps file access inside the workspace, `pilearn_wiki` is the only network access, limited to Wikipedia and Wikidata, and `pilearn-date` gives the model the date. `bash` is off.
- `/go` moves between levels, `/reader-model` picks the model that reads PDFs, and `/history` tells the story behind an idea.

## Questions and cards

- Questions ask for free recall first, like "state the mechanism in your own words". Multiple choice only appears to tell two easily confused ideas apart. The target is questions the learner can almost answer.
- Each Anki card holds one item and asks for recall, with "explain why X", "derive Y", or "compare A and B", never recognition or trivia.
- New cards come only from what the learner got wrong or recalled weakly that session. That keeps the deck small.
- At most 8 new cards per session. Anki's own "new cards per day" limit handles daily load.
- The measure is retention and the longest interval recalled, never cards reviewed or streaks.

## Evaluation

Each block gets a pre-test and a post-test with the same questions. Warm-ups and `/review` measure recall later, grouped by time since study. Anki reports retention over 30 days. `/progress` shows all three. There is no comparison with the old multiple-choice setup, which no longer exists, and no feel ratings, which nobody used.

## Open questions

- How much checking of the digests is enough? The first 14 were spot-checked against page images.
- Windows support hasn't been tested on a Windows machine.
- How big a subagent budget per session is worth it before it slows the loop?

## Decision log (append-only)

- **2026-09-21 · v0 created** after Day 1 + Kestin 2025 quick read. Principles P1–P9 set. Book-chunked protocol adopted as v0 answer. MC-only flaw logged with P2 fix. Installation = Feynman-like pattern. Evaluation = mini-RCT on self vs. current MC-only setup.
- **2026-09-21 · P10 + reuse.** Added P10: subagents run async in a separate WezTerm tab (visible parallel progress: the author's preferred behavior from their global Pi setup). Added reuse principle: the learning tool should reuse Feynman's research kit (alpha paper search, web search, document parsing, fact-checking, plot subagents) rather than reimplement it.
- **2026-09-22 · Day 2 (Freeman 2014) calibers.** Meta-analysis effect 0.47 SD ≈ Day 1's own 0.46; failure rate halved; instructor-equivalence subgroup *g*=0.49 rules out the "good teacher" confound; concept-inventory gains > course-exam gains; largest gains in classes ≤50. Adopted into tool: active = activities/discussion + higher-order thinking + group work (not recall questions); assess with conceptual measures; design around pair/small-group work.
- **2026-09-22 · Anki bridge (P11).** Yes to the bridge, with guardrails. Anki = retrieval/spacing engine for *atomic items*; synthesis stays in tutor sessions (Kestin boundary: weak for complex integration). Added P11 + architecture node (proposed via AnkiConnect localhost HTTP or file/CSV export: mechanism OPEN, verify at build). Card-authoring spec v0 added: atomic cards, recall-not-recognition, error-driven production, desirable-difficulty targeting, ≤5–8 new cards/lesson cap, retention (not activity) as the metric. This is also the tool's objective retention-measurement layer (P7 evaluation).
- **2026-09-22 · Build-critical subset done + Anki source clarified.** Track A complete: read ICAP (Chi & Wylie 2014), Roediger & Karpicke 2011, Bjork & Bjork 2011, Kirschner/Sweller/Clark 2006; adopted P12–P14 (guided interactive-constructive; test-don't-re-present; desirable difficulties in schedule). Clarification (the author): do NOT follow the Anki instructions/decks in an older personal study plan: that part was a personal guideline, not science-based. Our cards use the design-doc card-authoring spec (science-based), not premade decks. Book of Proof (Hammack) adopted as Block 0 / first study text.
- **2026-09-22 · v0 launcher + installer built.** New clean root `~/pilearn/` (separate from global `~/.feynman` and an older PILearn prototype). Built + tested `pilearn` launcher (cwd-detects course vs overarching; `--course <name>` override; `exec feynman` as borrowed runtime) and `install.sh` (creates `~/.pilearn`, installs `pilearn` into `~/.local/bin` on PATH). Update model: thin launcher reads harness from disk each launch → edits live, no update step in dev; install.sh idempotent. OPEN/honest: hosted `curl|bash` URL needs a host (local install.sh is the identical mechanism); deep runtime separation (pilearn loading own skills without touching global pi) is the open wiring item.
- **2026-09-22 · Architecture correction (the author): pilearn is a SIBLING on Pi, not a feynman session.** Pi = extensible core; feynman = one curated package; pilearn = another package (tutor/learning), reusing skills/tools from feynman + ecosystem by copying/adapting, not building from scratch and NOT an `exec feynman` session. Recorded as the Stack & packaging model. `exec feynman` launcher marked STOPGAP; next step = study how feynman packages on Pi (entrypoint/skills/config) so pilearn mirrors it as its own package entrypoint.
- **2026-09-22 · Pi packaging mapped + pilearn sibling package v0 built.** Verified the Pi mechanism: `getAgentDir()` uses `PI_CODING_AGENT_DIR` env else `~/<piConfig.configDir>/agent` (feynman sets `.feynman`); the harness is the standalone `pi` CLI (pi-coding-agent). Built pilearn as a true sibling: `~/pilearn` Node package with `piConfig {name: pilearn, configDir: .pilearn}`, `bin/pilearn.js` launcher (sets `PI_CODING_AGENT_DIR=~/.pilearn/agent`, resolves course vs overarching cwd, prints π learn banner, bootstraps the Pi harness from pi-coding-agent reused as a dep), `install.sh` (installs `pilearn` into `~/.local/bin`, copies agent skeleton), `scripts/postinstall.mjs`, `seeds/settings.json`. Seeded `~/.pilearn/agent` with 48 copied ecosystem skills, 4 subagents (researcher/reviewer/verifier/writer), theme `pilearn`, and models/auth/keybindings so the shared provider works. Note: models.json/auth.json are currently COPIED from feynman: pilearn should own its own model auth later (`pilearn model login`). Science kept in mind: `settings.json` defaultThinkingLevel, themed harness; tutor loop + card spec remain the learning-science core in SKILL.md.
- **2026-09-23 · Standalone package completed; engine swapped.** The `@mariozechner/pi-coding-agent` dependency was abandoned upstream (frozen at 0.73.1 since 2026-05-07); switched to `@earendil-works/pi-coding-agent` (0.87.1), the maintained continuation that Feynman runs. The repo's `agent/` tree (theme, subagents, skills, extensions) is now the single source of truth. `install.sh` and `scripts/postinstall.mjs` always sync it into `~/.pilearn/agent` and seed `settings/models/auth.json` only if missing. `seeds/models.json` resolves the "models.json COPIED from feynman" note: the API key is read from a shared env file, so PILearn no longer needs Feynman installed. The `exec feynman` stub was deleted. Verified by installing fresh into an empty directory and booting from it. Naming: PILearn in prose and branding; `pilearn` for the binary, folders, and config identifiers (same convention as Feynman). Added `~/pilearn/CLAUDE.md` with a process rule from the author: when extending the harness, read the package's `docs/` and `examples/extensions/` before reverse-engineering Feynman's bundled app.
- **2026-09-23 · Look and startup.** Theme reskinned to Tokyo Night. It had never been applied: the `"-themes/pilearn.json"` entry inherited from Feynman's settings uses Pi's `-` prefix, which *disables* a resource, so every session fell back to the built-in dark theme. Fixed. Feynman's startup panel turned out to be Feynman's own extension (`research-tools/header.ts`), not a Pi built-in. PILearn now has its own at `agent/extensions/pilearn-header.ts`, built on the documented `ctx.ui.setHeader()` API: block-letter logo, then model, directory, session, system, tool and agent counts, the agent list, and the workflows list, colored through theme tokens. Also fixed: `pilearn-tutor/SKILL.md` was missing frontmatter and showed a "skill conflict" error on every start.
- **2026-09-23 · Skill set pruned 48 → 12.** Kept: `pilearn-tutor`, `pdf-explore`, `alpha-research`, `deep-research`, `eli5`, `preview`, `session-log`, `session-search`, `self-awareness`, `product-self-knowledge`, `customize`, `skill-creator`. Dropped the bio/protein-ML skills, remote compute, paper authoring and review, literature-review/source-comparison, watch/jobs, and figure/plot. Rationale: the book is the ground truth (P5, P8), so tools that pull the tutor into broad literature surveys work against "don't get lost". This narrows the 2026-09-21 reuse note: plot/figure subagents are no longer part of the kit. Rebranded the Feynman identity strings inside the kept skills and subagents.
- **2026-09-23 · Three-tier context hierarchy + chapter-reader (the author's design).** The book is the primary source, and each tier reads only the *synthesized* output of the tier below: (1) a chapter-reader subagent (`agent/agents/chapter-reader.md`) runs in fresh context once per chapter, reads only that chapter's pages via `pdf-explore`, and writes one compact, page-anchored `scaffold/chNN.md` (objectives, key terms, pre-questions, content summary, exercise map); (2) the course session reads only that digest and runs the live tutor loop; (3) the aggregate session reads only `courses/*/progress.md`. Refinement agreed with the author: chapter-reader *digests, never tutors*, because the live retrieval dialogue (P2, P12) has to stay in the continuous course session. This implements Content strategy step 1 and the context budget rule, and it uses the `pi-subagents` fanout (fresh context, one output file per child) that is already installed.
- **2026-09-23 · Known gaps (OPEN).** No learning workflows yet: the header's Workflows column only lists the generic `pi-subagents` commands. Candidates: `/new-course`, `/prep-chapter`, `/study`, `/recall`. chapter-reader has never run, and `courses/bookofproof/` is empty (no PDF yet). Extension packages (`pi-subagents` etc.) load from the shared global npm root `/opt/homebrew`, not an isolated prefix like Feynman's; the missing `@earendil-works` peer packages were installed there. `seeds/models.json` hardcodes the author's home path, which blocks handing the package to students. Anki mechanism, `aggregate/` dashboard, and eval loop are still unbuilt.
- **2026-09-23 · Code/workspace split + per-level AGENTS.md (the author's design).** Pi loads `AGENTS.md` from the agent dir, the working directory, *and every parent directory*, so developer notes at the repo root were leaking into every tutoring session (the old `CLAUDE.md` already was). Split: `~/pilearn` holds package code only (developer `AGENTS.md`; `CLAUDE.md` retired, since Claude Code reads `AGENTS.md` natively). Study data moved to the workspace `~/study` (`$PILEARN_WORKSPACE`). Each level folder has its own `AGENTS.md`, and the stack reads as "the deepest one sets your role": level 1 `~/study/` (curriculum), level 2 `courses/<course>/` (tutor for one book), level 3 `courses/<course>/chapters/<chNN>/` (chapter-reader writes `digest.md`). The shared level map is installed as `~/.pilearn/agent/AGENTS.md`. The templates live in `agent/templates/` and are filled in by a deterministic `pilearn_scaffold` tool (P4: structure enforced by the tool, not the prompt). The tool validates ids, never overwrites, and records the source in `course.json`. Chapter digests moved from `scaffold/chNN.md` to `chapters/<chNN>/digest.md`, and course session logs from `digest/` to `sessions/`. The startup header now shows the level.
- **2026-09-23 · Study engine v1: math reading, book setup, `/study`, Anki bridge.** (1) *Reading math:* measured on Book of Proof p. 25: the PDF text layer (liteparse/PDFium via `pi-docparser`) keeps formulas but maps TeX symbols to the wrong characters (∑→`X`, ⋃→`[`, braces→`©ª`, flattened subscripts). `glm-5.3-flash` given a rendered page produced exact LaTeX. The chapter-reader now runs on `glm-5.3-flash`: text for prose, rendered pages for math, all math stored as LaTeX, so the vision cost is paid once per chapter (the deepseek endpoint rejects images). `pdf-explore` (Feynman leftover) replaced by `pi-docparser`'s tools. (2) *Math display:* Pi's Markdown renderer already turns `$…$`/`$$…$$` into Unicode math; the shared `AGENTS.md` now requires LaTeX delimiters for all math. (3) *Book setup:* `/add-book` reads the printed table of contents, finds the page offset by locating chapter starts, confirms with the learner, and asks when the TOC is unclear. `pageOffset` is stored in the course's `course.json` (level 2); chapters are created from printed page ranges. The source is symlinked into the course as `source.pdf`. Book of Proof set up this way: offset 12, verified on chapters 1, 10, and 14. (4) *Session loop* rewritten as 8 turn-gated steps with completion criteria, driven by `/study`, plus a fixed `progress.md` format whose recall log feeds warm-ups. (5) *Anki (P11), via AnkiConnect:* cards are created only from session misses and partials, at most 8 per session (enforced by the tool); daily intake is left to Anki's new-cards/day setting (the author's call: a per-day creation cap would drop valid cards on heavy study days). Review history is read back for the retention metric (P7). Session questions (understanding, graded by the tutor) and Anki cards (atomic items, self-graded) stay distinct, per P11.
- **2026-09-23 · First digest; study tree; install fixes.** The university service's vision model was too slow during the day (about a minute per round of page images), so the author had the ch01 digest written by Claude Code, reading pages 15–45 as images and following `chapter-reader.md`'s format exactly. The structure is indifferent to who writes `digest.md`; `/prep` stays the in-PILearn path. The startup header now shows the study tree next to the panel: courses always visible, the current course expanded, the current course or chapter marked `◀`, and chapters shown as studied ✓, prepared, or dimmed if not prepared. The Workflows list shows only PILearn's own commands. Installer fix: the level-1 `AGENTS.md` is PILearn's own instructions, so it is now always synced (it had been seeded once and went stale). Course- and chapter-level `AGENTS.md` files are filled in per course and are not regenerated when templates change (OPEN if that becomes a problem).
- **2026-09-23 · Audit + `/go`.** A full audit found inherited Feynman parts that pointed at things PILearn doesn't have. `alpha-research`, `eli5`, and the `researcher` agent all relied on a `pilearn alpha` CLI; the alphaXiv tools come from Feynman's bundled `@companion-ai/alpha-hub`, which was never installed. `deep-research`/`session-log`/`preview` ran Feynman slash commands that don't exist in PILearn, and session-log wrote to `notes/session-logs/`, competing with `sessions/`. `self-awareness`/`product-self-knowledge` described Feynman's workbench. `customize`/`skill-creator` edited `~/.pilearn/agent/`, which the installer overwrites. The researcher/reviewer/verifier/writer agents were a paper-research pipeline that PILearn never dispatches. All of these were removed; `eli5` was rewritten as a book-grounded, attempt-first explainer. What remains: skills `pilearn-tutor`, `eli5`, `session-search` (plus the packages' `parse-document` and `pi-subagents`), and one agent, `chapter-reader`. Web lookups use `pi-web-access`'s `web_search`. Other fixes: keybindings added to the seeds; the dead `~/.pilearn/root` file removed. New `/go` command: an arrow-key picker (`ctx.ui.select`) for level 1 or any course. A Pi process can't change its working directory, which is what determines the level, so `/go` writes the target to a handoff file, shuts Pi down, and the launcher relaunches Pi there. Level 3 is never entered by hand; it is only where chapter-readers run.
- **2026-09-23 · Evaluation loop built into `/study`; web search removed.** The evaluation plan lives inside the session loop, so data is recorded from the first session on. Pre-questions are scored as a pre-test (step 3). The same questions are asked again after reading as a post-test, and the change is shown to the learner (step 5, which also serves P9's early, visible assessment). Four 1–5 perception ratings (engagement, motivation, enjoyment, growth mindset) are logged each session and never used to judge progress (P7). New `progress.md` columns: Pre-test, Post-test, Warm-up, Feel, Cards. `/review` is a recall-only session across studied chapters (plan item 3: spaced recall over prior lessons; P13/P14). `/progress` reports pre→post gains, retention by time since study, Anki retention, and feel vs. recall. Dropped: plan item 4 (comparison against the old MC-only setup), which would need a controlled experiment against a setup that no longer exists. The author's call: web and paper search (`pi-web-access`) removed from PILearn. Feynman is the research tool, papers enter PILearn as PDFs via `/add-book`, and a tutor that can't search the web can't drift from the book (P5). Also less to maintain.
- **2026-09-23 · `bash` disabled in PILearn sessions (the author's call).** Removing web search left one way to the internet: the general shell tool. PILearn's settings now set `defaultTools: read, edit, write, grep, find, ls`, so `bash` is off. This is a setting rather than an instruction, so the model can't re-enable it; the learner's own `!command` shell shortcut is unaffected. `grep`/`find`/`ls` search local files only; `session-search` was rewritten to use them, checking `progress.md`/`sessions/` first. The resulting tool set is local files, document parsing, scaffold, Anki (localhost), and subagents. Grounding in the book remains an instruction (P5), made checkable by page references.
- **2026-09-23 · Portable package, published (private).** Made PILearn installable on any machine. `install.sh` is the single install path: it checks Node 22.19+, runs `npm install`, syncs `agent/`, seeds settings/models/auth/keybindings if missing, runs `pi install` for each extension package into PILearn's own folder (`~/.pilearn/agent/npm`, no global npm dependency), creates the workspace, and installs the launcher. The npm postinstall duplicate was removed. The API key in the seeded `models.json` is an environment variable instead of a personal file path, and the Pi dependency is pinned to `^0.87.1` rather than `latest`. Verified with a fresh install into an empty directory, then on the author's machine. Secret check before publishing: no keys, credentials, or personal paths in the repo; commits use the GitHub noreply address. The design notes moved into the repo (`docs/design-notes.md`), with a symlink at the old `~/outputs` path so Feynman sessions keep editing the same file. Repo: `github.com/Gurin808/pilearn`, private.
- **2026-09-23 · Exercises by hand; P15; README science section; citation corrections.** Practice was redesigned by the author: complex math isn't done in the chat. The tutor walks one worked example as reasoning, then assigns exercises to do by hand, chosen deliberately (like cards): weighted toward misses, partial answers, and exercises reported hard; at least one hard problem; preferring exercises covered by the source's own solutions. The count comes from a per-course `practice` setting (`foundational` about 4–6, `conceptual` about 1–3). The model may solve an exercise on request after an attempt, and points to the book's solution for cross-checking. `course.json` gains `practice` and `solutions` (Book of Proof: foundational; odd-numbered exercises, printed pp. 292–364). Warm-ups open by asking how the exercises went, and progress.md gains an Exercises table. New principle P15 (understanding over computation; hard problems as means), recorded as the author's stance with supporting evidence; the tutor skill gains a "What the learning is for" section. The README gains a quote, a "Why PILearn" section, and "The science behind PILearn": every reference was checked against Crossref by DOI (Bjork & Bjork 2011 by publisher record; the Bastani et al. 2025 correction only fixes an affiliation). Corrected mis-citations in the principles table: "Roediger & Karpicke 2011" does not exist (now Roediger & Karpicke 2006; Roediger & Butler 2011; the 2026-09-22 log entry keeps its original wording), "Carpenter et al. 2023" → Pan & Carpenter 2023, "Kornell 2009" → Kornell, Hays & Bjork 2009. P9's day-one question is closed by the README.
- **2026-09-23 · Models: login first, reader model selectable.** The author switched to a ChatGPT subscription through Pi's `/login` (faster, more context, better at reading textbooks). The university model service stays configured in the local `models.json` but is no longer the default. New installs seed no provider: the installer's next steps are `/login`, `/model` (saved as default by Pi), `/reader-model`. The chapter-reader's hardcoded `model:` line is gone. `/reader-model` (extension) lists available image-capable models and stores the choice as a pi-subagents agent override (`subagents.agentOverrides.chapter-reader.model` in settings.json); such overrides apply to custom agents only when the agent file has no `model:` line, and they survive reinstalls. Pi's settings writes only touch fields Pi itself changed, so the override isn't clobbered. With no override, the chat model reads PDFs. The startup panel shows a `reader` line.
- **2026-09-23 · Always-loaded teaching core.** Skills load on demand, so outside `/study` the full `pilearn-tutor` procedure may never be read. A short "How to teach" section now sits in the shared `AGENTS.md`, which is always in context at every level: book first (P5), attempt first on problems with graduated hints (P3/P4), recall before explanation (P1), understanding over calculation (P15), judge by recall (P7). Lookups (a definition, a notation) get a direct answer from the book plus a recall check. Tested: in an interactive session "Why does a set with n elements have 2^n subsets?" got a hint and a question back rather than the proof. Single-answer `--print` mode still answers directly, which is expected, since there's no turn to reply in.
- **2026-09-23 · Sessions by section blocks; digests by section.** The author pointed out that Book of Proof has an exercise set after every section (Chapter 1 has 10 sections), not only at the chapter's end, and a chapter can run to 48 pages. The chapter-reader's digest is now organized by section: a *Sections* table (section, title, pages, exercise set), chapter-level objectives and pre-questions (tagged by section), then per section its key terms, content, worked examples, and exercises as printed; about 3,500 words for long chapters. The tutor reads the whole chapter digest every session (the author: the chapter tutor knows all of the chapter, aware of its sections); a `/study` session teaches a block of consecutive sections (about 20 printed pages, 1–4 sections), so long chapters take several sessions; pre-test, checks, and exercises come from the block, and exercises are cited as "Section 1.3, #5". `progress.md` columns say *Sections* instead of *Chapter*, and *Next* names the next block. Digests for all 14 chapters of Book of Proof were then written in this format by Claude Code subagents reading page images, one per chapter.
- **2026-09-24 · Windows install, folder guard, historical context.** Three additions from the author.
  - Cross-platform installer. `install.mjs` (Node) replaces the shell logic and runs on macOS, Linux, and Windows (`node install.mjs`); `install.sh` is a thin wrapper around it. On Windows the launcher is `%USERPROFILE%\.pilearn\bin\pilearn.cmd`. `/add-book` falls back from a symlink to a hard link, then to a copy, because Windows allows symlinks only with Developer Mode. The launcher's path check uses the platform separator. Tested on macOS with a fresh install; not yet tested on a Windows machine.
  - Folder guard (`pilearn-guard`). A `tool_call` handler checks every file-touching tool before it runs. Writes are allowed only inside the workspace. Reads are allowed in the workspace, in each course's source file (from `course.json`, usually outside via symlink), in the agent dir (skills, templates, past sessions) except `auth.json` and `models.json`, and in the temp dir where page screenshots land. Subagents may only start inside the workspace. Paths are resolved through symlinks. `document_*` calls with a remote OCR server are blocked, since that would send pages off the machine. Pi's docs are explicit that this is a guard on tools, not an OS sandbox; with `bash` disabled the tools are the model's only file access. Tested live: `/etc/hosts` and `~/Documents` were blocked, while the course PDF (a symlink to a folder outside the workspace) was allowed.
  - Historical context (`historical-context` skill, `/history`, `pilearn_wiki` tool). The author learns better with the story behind an idea: the problem or experiment that led to it, the people, and their teacher–student lineages. `pilearn_wiki` is PILearn's only network access, fixed to `<lang>.wikipedia.org` and `www.wikidata.org`. It offers `search`; `article` (the intro and section list, or one section); and `people` (Wikidata: dates, doctoral advisors and students, teachers, influences, employers, fields). Evidence note: history that explains *why* an idea exists gives meaning (P15), but details that don't serve the idea ("seductive details") can hurt learning (Harp & Mayer, 1998, verified via Crossref). So `/history` is on request, the tutor only offers it in one line after free recall, and every story ends by tying the history back to the definition or theorem. Math content still comes only from the book; historical facts cite their Wikipedia URLs, and anything that comes only from the model's own knowledge is marked as such. Tested live with Cantor (advisors Weierstrass and Kummer from Wikidata, tied back to Book of Proof pp. 269–282).
- **2026-09-24 · Storytelling style for `/history`.** The author's models are 3Blue1Brown and Steven Strogatz. The skill describes their qualities instead of asking for imitation: open with a puzzle, not a biography; tell the discovery so the idea seems to grow out of the problem; human and warm, addressed to the learner; concrete images; wonder without hype.
- **2026-09-24 · Feel ratings dropped; date in the prompt; generic README example.** The four 1–5 ratings (engagement, motivation, enjoyment, growth) are removed from the session loop, `progress.md`, and `/progress`. They were only ever compared with recall, not used, and with no study to feed there's no reason to ask them every session. P7 stays as a rule: progress is judged only by recall. The first three real sessions (2026-09-23) logged "date unavailable": Pi doesn't put the date in the system prompt, and with `bash` off the model can't look it up. `pilearn-date` now appends today's date in `before_agent_start`. The existing log was backfilled from session-file timestamps. The README's custom-provider example is now a generic placeholder, and the install steps use the repo URL.
- **2026-09-24 · Style pass over every instruction.** Models copy the style of what they read, so the tutor's instructions shouldn't carry AI tells. The README, the developer notes, every template, skill, prompt, and agent file, and the model-facing strings in the extensions were rewritten in plain sentences: no em dashes, no bold labels followed by a colon, no filler. Principle numbers like "(P7)" came out of runtime files, since the tutor can't read this file anyway; the principles table above keeps the link to the evidence. The sections above the log were brought up to date with what PILearn does now. In the log itself only punctuation and bold changed, not what the entries say. The chapter-reader also gained a rule from the Book of Proof prep: use the book's own labels, refer to unnumbered items by page, and say when an exercise grouping is the reader's own.
- **2026-09-24 · Only PILearn's own skills; unslop bundled.** Pi also loads skills from the shared `~/.agents/skills` folder that other agent tools use. In PILearn they showed up in the skill list, but the guard blocked reading them, because they sit outside the allowed folders. `settings.json` now excludes that folder with the pattern `!**/.agents/skills/**`, and the installer adds it to existing installs too. The author wanted the `unslop` writing skill, so it's bundled in `agent/skills/unslop` under its MIT license (pstack, Lauren Tan; see `THIRD_PARTY_NOTICES.md`), and the shared `AGENTS.md` points to it for plain writing. Verified: the model lists eli5, historical-context, pilearn-tutor, session-search, unslop, and the two package skills, and nothing else.
